program fixed_test;

{$mode objfpc}{$H+}
uses fixedint;

var x, vx: fix32;
  var v: Vec2D_f32;
begin
  x := 0;
  vx := fix32Div(intToFix32(-1), intToFix32(32));
  writeln(vx);

  v.x

  //writeln(floatToFix32(12.34));
  //writeln(floatToFix32(1));

  //writeln(fix32Mul(-1, 512));
  //writeln(fix32ToFix16(-1024));
  readln;
end.

